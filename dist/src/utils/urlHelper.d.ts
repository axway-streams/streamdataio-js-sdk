export declare class UrlHelper {
    static urlToString(protocol: string, hostname: string, port?: string | number): string;
    private static _isEmptyPort;
    private static _isNumber;
    private static _isString;
}
